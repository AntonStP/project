include ../build/build.pug

html
  head
    link(rel="stylesheet" href="css/styles.css")
    title Страница
  body
    +page()
      +main-wrapper()
        +header()
        +section()
          +image()
          +article(pageHeading,pageContent)
      +right-wrapper()   
        +aside-navigation()
          +menu(menuData)
          +button-block()
        +footer()
          +download()
          +copyright()