include ../../build/build.pug

//- mixin pageGenerator(pageH,pageCont,numberPage,menuD)
block vars
    //- -
        //- var pageH = pageHeading.title_1;

html
    head
        link(rel="stylesheet" href="css/styles.css")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(name="format-detection" content="telephone=no")
        title Raiffeisen Bank
    body
        +page()
            +main-wrapper()
                +header()
                block content
            +right-wrapper()   
                +aside-navigation()
                    +menu(menuData,currentPage)
                    +button-block()
                +footer()
                    +download()
                    +copyright()
    //- block